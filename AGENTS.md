# AGENTS.md

## 1. 프로젝트 개요

이 저장소는 `ending_credit` Java Spring Boot 웹 애플리케이션이다.

현재 기준 환경:

- Java: 21
- Spring Boot: 3.5.16
- 빌드: Maven / Maven Wrapper
- 영속성 처리: MyBatis
- 데이터베이스: Oracle
- 기본 패키지: `com.endit`
- Domain VO 클래스: `src/main/java/com/endit/domain`
- Mapper 인터페이스: `src/main/java/com/endit/mapper`
- MyBatis Mapper XML: `src/main/resources/mapper`
- Mapper DAO 테스트: `src/test/java/com/endit/mapper`

이 문서가 최신 코드 상태를 완벽하게 반영한다고 가정하지 말고 항상 현재 저장소를 먼저 확인한다.

---

## 2. 기존 프로젝트 동작 유지

사용자가 명시적으로 요청하지 않는 한 기존 기능을 변경하지 않는다.

작업에 반드시 필요한 경우가 아니라면 다음 항목을 변경하지 않는다.

- 실행 순서
- 반환값
- SQL 동작
- 어노테이션 값
- URL 경로
- 패키지명
- 클래스명
- 메서드명
- 필드명
- 문자열 값
- 데이터베이스 테이블명
- 데이터베이스 컬럼명

요청받은 변경을 구현하면서 관련 없는 리팩터링을 수행하지 않는다.

요청을 완전히 충족하는 범위에서 가장 작은 변경을 우선한다.

---

## 3. Java 코딩 규칙

### Lombok

새로운 Lombok 사용을 추가하지 않는다.

다음과 같은 어노테이션을 추가하지 않는다.

- `@Data`
- `@Getter`
- `@Setter`
- `@Builder`
- `@RequiredArgsConstructor`
- `@AllArgsConstructor`
- `@NoArgsConstructor`
- `@Slf4j`

Java 클래스를 생성하거나 수정할 때는 다음 항목을 명시적인 Java 코드로 작성하는 방식을 우선한다.

- 생성자
- getter
- setter
- `toString()`
- SLF4J Logger 선언

프로젝트에는 다른 팀원이 작성한 Lombok 코드나 Lombok 의존성이 여전히 존재할 수 있다.

다음 작업은 임의로 수행하지 않는다.

- Lombok 의존성 자동 삭제
- 관련 없는 기존 Lombok 코드 재작성
- 명시적인 요청 없이 다른 팀원이 작성한 Lombok 코드 수정

### 로깅

SLF4J를 직접 사용한다.

```java
private static final Logger log =
        LoggerFactory.getLogger(ClassName.class);
```

`@Slf4j`를 사용하지 않는다.

프로젝트 규칙을 따르는 DAO 테스트 메서드는 기존의 3줄 메서드 시작 박스 로그 형식을 유지한다.

```java
log.debug("┌──────────────────────────────┐");
log.debug("│ methodName()                 │");
log.debug("└──────────────────────────────┘");
```

간격은 기존 파일 스타일을 유지하는 데 필요한 범위에서만 조정한다.

### 일반 Java 클래스 멤버 정렬 순서

일반 Java 클래스는 기존 파일과 충돌하지 않는 범위에서 다음 순서를 우선한다.

1. 상수
2. Logger
3. 주입 필드
4. 일반 필드
5. 생성자
6. public 메서드
7. private 메서드

이 규칙만 적용하기 위해 관련 없는 기존 파일의 순서를 재정렬하지 않는다.

### VO 멤버 정렬 순서

VO/domain 클래스는 다음 순서를 우선한다.

1. 필드
2. 생성자
3. getter와 setter
4. 기타 메서드
5. `toString()`

작업상 필요한 이유가 없다면 필드명이나 데이터 타입을 변경하지 않는다.

---

## 4. 주석 및 문서화

기존 Javadoc 및 Modification History 규칙을 유지한다.

기존 파일을 수정할 때:

- 이유 없이 유용한 주석을 삭제하지 않는다.
- 주석이 실제 동작과 일치하도록 유지한다.
- 요청받은 코드 변경으로 기존 주석이 부정확해지면 함께 수정한다.
- 단순한 코드에 불필요하게 많은 설명 주석을 추가하지 않는다.

파일에 이미 Modification History 영역이 있다면 기존 형식을 따른다.

실제로 수행하지 않은 작업에 대한 과거 이력을 임의로 작성하지 않는다.

---

## 5. Spring Boot 규칙

- 기존 Spring Boot 프로젝트 구조를 유지한다.
- 명시적으로 요청하지 않는 한 새로운 아키텍처 계층을 추가하지 않는다.
- 현재 사용처를 확인하지 않고 component scanning, bean 등록, profile 또는 application 설정을 변경하지 않는다.
- 구현을 단순화한다는 이유만으로 새로운 의존성을 추가하지 않는다.
- 명시적으로 요청하지 않는 한 Spring Boot, Java, Maven 플러그인 또는 의존성 버전을 업그레이드하지 않는다.

저장소에는 과거부터 존재하는 이름 불일치가 있을 수 있다.

예를 들어 `com.endit.cofig` 같은 패키지가 현재 존재한다면:

- 현재 저장소의 실제 상태로 취급한다.
- 자동으로 이름을 변경하지 않는다.
- 의심되는 이름 문제를 별도로 보고한다.
- 명시적인 요청을 받은 경우에만 모든 참조를 확인한 후 이름을 변경한다.

---

## 6. MyBatis 규칙

Mapper 인터페이스와 Mapper XML은 항상 서로 일치하도록 유지한다.

Mapper 관련 기능을 수정할 때:

1. Mapper 인터페이스를 확인한다.
2. 대응하는 Mapper XML을 확인한다.
3. 관련 VO/domain 클래스를 확인한다.
4. 관련 DaoTest를 확인한다.
5. 해당 메서드가 이미 다른 곳에서 사용된다면 호출부도 확인한다.

다음을 검증한다.

- XML `namespace`가 Mapper 인터페이스와 일치하는지
- statement `id`가 Mapper 메서드와 일치하는지
- parameter type이 호환되는지
- result type 또는 result mapping이 호환되는지
- Oracle SQL 문법이 유지되는지
- 테이블명과 컬럼명이 현재 프로젝트 스키마와 일치하는지

다음 작업을 임의로 수행하지 않는다.

- 스타일 개선만을 위한 기존 SQL 재작성
- 요청받지 않은 쿼리 의미 변경
- Mapper 메서드 이름 임의 변경
- 추측에 따른 테이블명 또는 컬럼명 변경
- 명시적인 요청 없이 데이터베이스 스키마 변경 생성

Mapper 메서드를 추가하는 경우 작업에 필요하다면 대응하는 XML과 관련 테스트도 함께 수정한다.

---

## 7. Oracle 및 데이터베이스 안전 규칙

저장소에 존재하지 않거나 사용자가 명시적으로 제공하지 않은 스키마 세부 정보를 추측하지 않는다.

다음 작업을 자동으로 수행하지 않는다.

- 테이블 생성
- 테이블 삭제
- 테이블 변경
- 시퀀스 변경
- 제약조건 변경
- 인덱스 변경
- 공용 더미 데이터 삭제
- datasource 자격 증명 변경
- 데이터베이스 연결 설정 변경

외래 키가 필요한 테스트에서는 기존 공용/테스트 데이터에 이미 존재하거나 사용자가 명시적으로 제공한 값을 우선 사용한다.

테스트를 통과시키기 위해 새로운 공용 데이터베이스 ID를 임의로 만들어 사용하지 않는다.

데이터베이스 의존 테스트가 실패하면:

1. 실패 내용을 확인한다.
2. 원인이 코드, SQL, 설정, 연결, 스키마 또는 테스트 데이터 중 무엇인지 판단한다.
3. 관련 없는 코드를 변경하기 전에 원인을 보고한다.

---

## 8. Mapper DAO 테스트 규칙

기존 Mapper DAO 테스트는 프로젝트에서 정한 테스트 구조를 유지한다.

일반적인 규칙:

- `@SpringBootTest`
- `@Transactional`
- `@BeforeEach`
- JUnit 5 assertion
- `@DisplayName`
- 프로젝트 로그 형식

데이터베이스 변경 내용을 유지하기 위한 목적으로 `@Transactional`을 제거하지 않는다.

Oracle 시퀀스 값은 일반적인 트랜잭션 롤백과 동일하게 롤백되지 않을 수 있다는 점을 고려한다.

명시적으로 요구되지 않는 한 특정한 다음 시퀀스 번호에 의존하는 테스트를 작성하지 않는다.

### 테스트 수정 시

- 관련성이 가장 높은 최소한의 테스트만 수정하거나 추가한다.
- 관련 없는 테스트 케이스를 유지한다.
- 실패 테스트를 통과시키기 위해 assertion을 약화하지 않는다.
- 이유를 설명하지 않고 실패하는 테스트를 삭제하지 않는다.
- 운영 코드나 테스트 예상값을 변경하기 전에 근본 원인을 파악한다.

### 테스트 실행

먼저 범위가 작은 관련 테스트를 실행하는 방식을 우선한다.

예:

```bash
./mvnw.cmd test
```

또는 Windows 환경에 적합한 특정 Maven 테스트 명령을 사용한다.

데이터베이스 접근이나 환경 설정 문제로 테스트를 실행할 수 없다면 테스트가 통과했다고 말하지 말고 해당 제한 사항을 보고한다.

실제로 성공적으로 실행하지 않은 테스트를 성공했다고 보고하지 않는다.

---

## 9. 의존성 규칙

작업에 명시적으로 필요한 경우가 아니라면 Maven 의존성을 추가, 삭제 또는 업그레이드하지 않는다.

의존성을 추가하기 전에:

1. JDK 또는 현재 프로젝트 의존성으로 필요한 기능을 이미 구현할 수 있는지 확인한다.
2. 새로운 의존성이 필요한 이유를 설명한다.

관련 없는 정리 작업의 일부로 `pom.xml`을 수정하지 않는다.

---

## 10. 파일 및 빌드 결과물 안전 규칙

명시적으로 요청하지 않는 한 다음과 같은 생성된 빌드 결과물을 수정하지 않는다.

- `target/`
- `bin/`
- 생성된 로그 또는 임시 출력 파일

빌드 결과물 파일을 소스 파일처럼 취급하지 않는다.

사용되지 않는 것처럼 보인다는 이유만으로 참조 관계를 확인하지 않고 프로젝트 파일을 삭제하지 않는다.

---

## 11. 프로젝트 전용 검토 체크리스트

요청받은 변경이 Java/MyBatis 영속성 코드를 건드리는 경우 해당되는 항목을 모두 확인한다.

- VO/domain 클래스
- Mapper 인터페이스
- Mapper XML
- DaoTest
- XML namespace
- statement ID
- parameter type
- result mapping/type
- Oracle SQL 문법
- 테이블명/컬럼명
- 관련 외래 키 전제조건
- 관련 테스트 데이터
- import 및 컴파일
- 최종 Git diff

---

## 12. 완료 조건

다음 중 해당되는 항목을 모두 충족해야 코딩 작업이 완료된 것으로 판단한다.

- 요청받은 변경이 구현됨
- 관련 없는 기존 동작이 유지됨
- 관련 VO/Mapper/XML/Test 계층이 서로 일치함
- 의도하지 않은 파일이 변경되지 않음
- 실행 가능한 경우 관련 테스트를 실행함
- 실패 및 제한 사항을 정확하게 보고함
- 최종 Git diff를 검토함
- 명시적으로 요청받지 않은 commit 또는 push를 수행하지 않음

검증을 완료할 수 없다면 무엇이 아직 검증되지 않았는지 정확하게 설명한다.

---

## 13. 프로젝트 지시사항 우선순위

이 저장소에서는 다음 순서로 지시사항을 따른다.

1. 사용자의 현재 명시적 요청
2. 적용되는 시스템 및 보안 요구사항
3. 이 프로젝트 전용 `AGENTS.md`
4. 전역 `~/.codex/AGENTS.md`
5. 현재 저장소의 기존 규칙

작업별 지시는 이 파일의 기본 규칙을 의도적으로 덮어쓸 수 있다.

지시사항이 충돌하거나 요청 범위가 불명확하다면 광범위하거나 파괴적인 변경을 추측해서 진행하지 않는다. 먼저 충돌 내용을 보고하거나 사용자에게 확인한다.