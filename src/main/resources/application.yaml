spring:
  # =========================================================
  # Application
  # =========================================================
  application:
    name: ending_credit

  # =========================================================
  # Profile
  # =========================================================
  profiles:
    active: app

  # =========================================================
  # OAuth2 Client (Google 로그인)
  # =========================================================
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope:
              - email
              - profile

  # =========================================================
  # Thymeleaf
  # 개발 환경에서는 cache 비활성화
  # =========================================================
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    cache: false
    encoding: UTF-8


# =========================================================
# MyBatis
# =========================================================
mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
  type-aliases-package: com.endit.domain

  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: false


# =========================================================
# Server
# =========================================================
server:
  port: 8080


# =========================================================
# Logging
# =========================================================
logging:
  level:
    root: INFO
    com.endit: DEBUG

    # Transaction
    org.springframework.transaction: DEBUG
    org.springframework.transaction.interceptor: DEBUG

    # DataSource TransactionManager
    org.springframework.jdbc.datasource.DataSourceTransactionManager: DEBUG

    # MyBatis
    org.mybatis: DEBUG

    # SQL
    org.springframework.jdbc.core: DEBUG

  # Log File
  file:
    name: logs/application.log

  # Log Pattern
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"


# =========================================================
# TMDB
# api-key는 TMDB Read Access Token(JWT). 라이브러리 요구 사항이라 v3 키가 아니라 이 토큰을 넣는다.
# =========================================================
tmdb:
  api-key: "eyJhbGciOiJIUzI1NiJ9.eyJhdWQiOiI4NTA4NjI4YjcwZjNiMzNiMmQ3NWE3Yjc3YWE2YzkzNCIsIm5iZiI6MTc4NzgxODcyMi4zMTksInN1YiI6IjZhOGZmMmUyMTllMWJiZjgwYzNjZWEzZSIsInNjb3BlcyI6WyJhcGlfcmVhZCJdLCJ2ZXJzaW9uIjoxfQ.r17zMgOys95XGC0ux2AO1LI1wU8JFM0FQKK-IiYWJS4"
  language: ko-KR
  image-size: w500
