<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.endit.mapper.UserCommentMapper">

    <!-- 검색조건: 목록조회/총건수 양쪽에서 같이 쓴다
         searchDiv 10: 작성자 회원ID, 20: 영화ID, 30: 컬렉션ID
         (조회가 join을 쓰므로 컬럼을 uc. 별칭으로 한정한다) -->
    <sql id="userCommentWhere">
        <where>
         <if test="searchWord != null and searchWord != ''">
             <choose>
                <when test=" searchDiv == '10' ">
                    uc.member_id = #{searchWord}
                </when>
                <when test=" searchDiv == '20' ">
                    uc.content_id = #{searchWord}
                </when>
                <when test=" searchDiv == '30' ">
                    uc.collection_id = #{searchWord}
                </when>
                <otherwise>

                </otherwise>
             </choose>
         </if>
        </where>
    </sql>

    <!-- PK는 DEFAULT가 없어서 시퀀스 값을 여기서 직접 채번해 넣는다 (안 넣으면 ORA-01400) -->
    <insert id="doSave" parameterType="UserCommentVO">
        <selectKey keyProperty="commentId" resultType="long" order="BEFORE">
            SELECT SEQ_USER_COMMENT.NEXTVAL FROM dual
        </selectKey>
        INSERT INTO user_comment (
            comment_id,
            member_id,
            content_id,
            collection_id,
            comment_detail,
            spoiler,
            created_dt
        ) VALUES (
            #{commentId},
            #{memberId},
            #{contentId,jdbcType=NUMERIC},
            #{collectionId,jdbcType=NUMERIC},
            #{commentDetail},
            #{spoiler},
            SYSDATE )
    </insert>

    <!-- 단건조회 — 화면설계서(C-04·D-07 카드) 기준 join (8/18 공지 보완점2)
         MEMBER: 작성자 닉네임(FK NOT NULL이라 JOIN)
         COMMENT_LIKE: 좋아요 수 집계(없으면 0)
         MEMBER_CONTENT: 작성자가 그 영화에 준 별점(별점 없거나 컬렉션 코멘트면 null) -->
    <select id="doSelectOne"
            parameterType="UserCommentVO"
            resultType="UserCommentVO">
         SELECT
             uc.comment_id,
             uc.member_id,
             uc.content_id,
             uc.collection_id,
             uc.comment_detail,
             uc.spoiler,
             TO_CHAR(uc.created_dt,'YYYY/MM/DD HH24:MI:SS') created_dt,
             TO_CHAR(uc.updated_dt,'YYYY/MM/DD HH24:MI:SS') updated_dt,
             m.nickname,
             m.profile_img_url,
             NVL(lc.like_cnt, 0) like_cnt,
             mc.rating_score,
             (SELECT MAX(rc.reason) FROM report_comment rc
               WHERE rc.comment_id = uc.comment_id
                 AND rc.status = 'ACCEPTED') AS blind_reason
         FROM
             user_comment uc
             JOIN member m ON m.member_id = uc.member_id
             LEFT JOIN (SELECT comment_id, COUNT(*) like_cnt
                          FROM comment_like
                         GROUP BY comment_id) lc
                    ON lc.comment_id = uc.comment_id
             LEFT JOIN member_content mc
                    ON mc.member_id = uc.member_id
                   AND mc.content_id = uc.content_id
         WHERE uc.comment_id = #{commentId}
    </select>

    <!-- 수정은 내용과 스포일러 여부만. 대상(영화/컬렉션)은 바꿀 수 없다 -->
    <update id="doUpdate" parameterType="UserCommentVO">
         UPDATE user_comment
         SET
              comment_detail = #{commentDetail},
              spoiler        = #{spoiler},
              updated_dt     = SYSDATE
         WHERE
                comment_id = #{commentId}
    </update>

    <!-- 좋아요·신고는 FK ON DELETE CASCADE로 함께 지워진다 -->
    <delete id="doDelete" parameterType="UserCommentVO">
        DELETE FROM user_comment
        WHERE comment_id = #{commentId}
    </delete>

    <!-- 목록조회 — rownum 3중 페이징 + join + 정렬 4종(C-04: 최신/좋아요/평점높은/평점낮은)
         정렬 키는 dto.searchMap의 "sort" (latest|likes|rating_desc|rating_asc, 그 외/없음=latest)
         별점 정렬은 NULLS LAST — 별점 없는 코멘트가 맨 위로 오지 않게 -->
    <select id="doRetrieve"
        parameterType="com.endit.cmn.DTO" resultType="UserCommentVO">
          SELECT A.*,B.*
            FROM(
              SELECT tt1.rnum no,
                     tt1.comment_id,
                     tt1.member_id,
                     tt1.content_id,
                     tt1.collection_id,
                     tt1.comment_detail,
                     tt1.spoiler,
                     TO_CHAR(tt1.created_dt,'YYYY/MM/DD HH24:MI:SS') created_dt,
                     TO_CHAR(tt1.updated_dt,'YYYY/MM/DD HH24:MI:SS') updated_dt,
                     tt1.nickname,
                     tt1.profile_img_url,
                     tt1.like_cnt,
                     tt1.rating_score,
                     tt1.blind_reason,
                     tt1.target_type,
                     tt1.target_title,
                     tt1.release_year,
                     tt1.collection_author_nickname,
                     tt1.liked_by_member
                FROM(
                      SELECT ROWNUM rnum, t1.*
                        FROM (
                              SELECT uc.*,
                                     m.nickname,
                                     m.profile_img_url,
                                     NVL(lc.like_cnt, 0) like_cnt,
                                     mc.rating_score,
                                     (SELECT MAX(rc.reason) FROM report_comment rc
                                       WHERE rc.comment_id = uc.comment_id
                                         AND rc.status = 'ACCEPTED') AS blind_reason,
                                     CASE
                                         WHEN uc.content_id IS NOT NULL THEN 'MOVIE'
                                         ELSE 'COLLECTION'
                                     END target_type,
                                     CASE
                                         WHEN uc.content_id IS NOT NULL
                                             THEN NVL(c.title_ko, c.title_org)
                                         ELSE col.title
                                     END target_title,
                                     TO_CHAR(c.release_year, 'YYYY') release_year,
                                     col_owner.nickname collection_author_nickname,
                                     <choose>
                                         <when test="searchMap.viewerMemberId != null">
                                             CASE WHEN EXISTS (
                                                 SELECT 1
                                                   FROM comment_like viewer_like
                                                  WHERE viewer_like.comment_id = uc.comment_id
                                                    AND viewer_like.member_id = #{searchMap.viewerMemberId}
                                             ) THEN 1 ELSE 0 END
                                         </when>
                                         <otherwise>0</otherwise>
                                     </choose> liked_by_member
                                FROM user_comment uc
                                JOIN member m ON m.member_id = uc.member_id
                                LEFT JOIN (SELECT comment_id, COUNT(*) like_cnt
                                             FROM comment_like
                                            GROUP BY comment_id) lc
                                       ON lc.comment_id = uc.comment_id
                                LEFT JOIN member_content mc
                                       ON mc.member_id = uc.member_id
                                      AND mc.content_id = uc.content_id
                                LEFT JOIN content c
                                       ON c.content_id = uc.content_id
                                LEFT JOIN collection col
                                       ON col.collection_id = uc.collection_id
                                LEFT JOIN member col_owner
                                       ON col_owner.member_id = col.member_id
                               <include refid="userCommentWhere"></include>
                               ORDER BY
                               <choose>
                                   <when test=" searchMap.sort == 'likes' ">
                                       like_cnt DESC, uc.created_dt DESC, uc.comment_id DESC
                                   </when>
                                   <when test=" searchMap.sort == 'oldest' ">
                                       uc.created_dt ASC, uc.comment_id ASC
                                   </when>
                                   <when test=" searchMap.sort == 'rating_desc' ">
                                       rating_score DESC NULLS LAST, uc.created_dt DESC, uc.comment_id DESC
                                   </when>
                                   <when test=" searchMap.sort == 'rating_asc' ">
                                       rating_score ASC NULLS LAST, uc.created_dt DESC, uc.comment_id DESC
                                   </when>
                                   <otherwise>
                                       uc.created_dt DESC, uc.comment_id DESC
                                   </otherwise>
                               </choose>
                       )t1
                    <![CDATA[    WHERE ROWNUM <= ( #{pageSize} * (#{pageNo} -1 ) + #{pageSize}  )  ]]>
                )tt1
             <![CDATA[   WHERE rnum >= ( #{pageSize} * (#{pageNo} -1 ) + 1    ) ]]>
           ) A CROSS JOIN (
              SELECT COUNT(*) total_cnt
                FROM user_comment uc
                <include refid="userCommentWhere"></include>
           ) B
    </select>

    <select id="totalCnt" resultType="java.lang.Integer">
        SELECT COUNT(*) AS total_cnt FROM user_comment
    </select>

    <!-- 검색조건이 걸린 건수 (doRetrieve의 CROSS JOIN 쪽과 같은 조건 조각을 공유한다)
         totalCnt는 조건 없는 전체 건수라 회원별·영화별 집계에는 쓸 수 없다.
         신고 승인으로 가려진 코멘트도 삭제된 것이 아니므로 건수에 포함한다. -->
    <select id="totalCntBySearch" parameterType="com.endit.cmn.DTO" resultType="java.lang.Integer">
        SELECT COUNT(*) AS total_cnt
          FROM user_comment uc
        <include refid="userCommentWhere"></include>
    </select>

    <!-- 화면 헤더용 대상 제목 (C-04 영화 요약 / D-07 컬렉션 정보) -->
    <select id="getContentTitle" parameterType="long" resultType="string">
        SELECT NVL(title_ko, title_org) FROM content WHERE content_id = #{contentId}
    </select>

    <select id="getCollectionTitle" parameterType="long" resultType="string">
        SELECT title FROM collection WHERE collection_id = #{collectionId}
    </select>

</mapper>
