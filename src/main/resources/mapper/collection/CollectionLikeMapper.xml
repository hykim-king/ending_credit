<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.endit.mapper.CollectionLikeMapper">

    <!-- COLLECTION_LIKE 테이블 컬럼과 CollectionLikeVO 필드 매핑 -->
    <resultMap id="collectionLikeResultMap" type="com.endit.domain.CollectionLikeVO">
        <id property="memberId" column="MEMBER_ID" />
        <id property="collectionId" column="COLLECTION_ID" />
        <result property="createdDt" column="CREATED_DT" />
    </resultMap>

    <!-- 컬렉션 좋아요 등록: CREATED_DT는 테이블 기본값 SYSDATE 사용 -->
    <insert id="insertCollectionLike" parameterType="com.endit.domain.CollectionLikeVO">
        INSERT INTO COLLECTION_LIKE (
            MEMBER_ID,
            COLLECTION_ID
        ) VALUES (
            #{memberId},
            #{collectionId}
        )
    </insert>

    <!-- 컬렉션 좋아요 삭제: 복합 PK 전체 사용 -->
    <delete id="deleteCollectionLike" parameterType="com.endit.domain.CollectionLikeVO">
        DELETE FROM COLLECTION_LIKE
         WHERE MEMBER_ID = #{memberId}
           AND COLLECTION_ID = #{collectionId}
    </delete>

    <!-- 회원 번호와 컬렉션 번호를 이용한 단건 조회 -->
    <select id="selectCollectionLike"
            parameterType="com.endit.domain.CollectionLikeVO"
            resultMap="collectionLikeResultMap">
        SELECT MEMBER_ID,
               COLLECTION_ID,
               TO_CHAR(CREATED_DT, 'YYYY-MM-DD HH24:MI:SS') AS CREATED_DT
          FROM COLLECTION_LIKE
         WHERE MEMBER_ID = #{memberId}
           AND COLLECTION_ID = #{collectionId}
    </select>

    <!-- 특정 회원이 좋아요를 누른 컬렉션 목록 조회 -->
    <select id="selectCollectionLikeListByMember"
            parameterType="int"
            resultMap="collectionLikeResultMap">
        SELECT MEMBER_ID,
               COLLECTION_ID,
               TO_CHAR(CREATED_DT, 'YYYY-MM-DD HH24:MI:SS') AS CREATED_DT
          FROM COLLECTION_LIKE
         WHERE MEMBER_ID = #{memberId}
         ORDER BY CREATED_DT DESC
    </select>

    <!-- 특정 컬렉션에 좋아요를 누른 회원 목록 조회 -->
    <select id="selectCollectionLikeListByCollection"
            parameterType="int"
            resultMap="collectionLikeResultMap">
        SELECT MEMBER_ID,
               COLLECTION_ID,
               TO_CHAR(CREATED_DT, 'YYYY-MM-DD HH24:MI:SS') AS CREATED_DT
          FROM COLLECTION_LIKE
         WHERE COLLECTION_ID = #{collectionId}
         ORDER BY CREATED_DT DESC
    </select>

    <!-- 특정 컬렉션의 좋아요 개수 조회 -->
    <select id="selectCollectionLikeCount"
            parameterType="int"
            resultType="int">
        SELECT COUNT(*)
          FROM COLLECTION_LIKE
         WHERE COLLECTION_ID = #{collectionId}
    </select>

</mapper>